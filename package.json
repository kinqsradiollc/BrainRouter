{
  "name": "brainrouter-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*", "mcp", "web", "brainrouter"],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present",
    "cli": "node brainrouter/dist/index.js"
  }
}
