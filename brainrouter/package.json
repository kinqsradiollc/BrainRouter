{
  "name": "brainrouter",
  "version": "0.2.0",
  "description": "BrainRouter Terminal Agent CLI — premium interactive terminal-based agent client.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "brainrouter": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@brainrouter/sdk": "*",
    "@brainrouter/types": "*",
    "@modelcontextprotocol/sdk": "^1.11.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "inquirer": "^9.3.2",
    "marked": "^12.0.1",
    "marked-terminal": "^7.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/marked": "^4.0.8",
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
