# Expo / RN
node_modules/
.expo/
dist/
web-build/
*.log

# Native
ios/
android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Host integration-test throwaway home (redirected HOME with a minimal config +
# migrated .brainrouter state). Regenerated by host/*.mjs on each run.
host/.test-home/
host/.host-home/
host/dist-verify/

# Env
.env*.local

# macOS
.DS_Store
