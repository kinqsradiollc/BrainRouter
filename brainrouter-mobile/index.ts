// Expo entry. registerRootComponent calls AppRegistry.registerComponent and
// ensures the app works in Expo Go and a native build alike.
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
