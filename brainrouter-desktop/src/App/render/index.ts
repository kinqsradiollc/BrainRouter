/**
 * App shell — the render-closure factories: the per-row sidebar node + transcript
 * MessageRow helpers, and the side/dock panel-body renderer.
 */
export * from './renderHelpers.js';
export * from './renderPanelBody.js';
