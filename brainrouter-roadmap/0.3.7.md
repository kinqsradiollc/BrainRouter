# 0.3.7 — Terminal UI redesign + in-terminal configuration + multi-agent registry

**Status:** Shipped 2026-05-26. Items 6–9 merged; Items 1–5 cut to 0.3.8.
**Full notes:** [`brainrouter-changelog/0.3.7.md`](../brainrouter-changelog/0.3.7.md)

| # | Item | Status |
|---|---|---|
| 1 | Cron-style `/schedule` | Cut → 0.3.8 |
| 2 | `/release-notes` in-CLI | Cut → 0.3.8 |
| 3 | Hooks JSON authoring doc | Cut → 0.3.8 |
| 4 | Strict Tool-Call Recovery (deer-flow pattern) | Cut → 0.3.8 |
| 5 | Per-vendor MCP install snippets | Cut → 0.3.8 |
| 6 | Terminal UI redesign + in-terminal config wizard | Shipped (scope expanded) |
| 7 | CLI Multi-Agent Phase 1 — registry foundations | Shipped |
| 8 | API key polish | Shipped |
| 9 | `/model` quick-swap picker | Shipped |

---

**6. Terminal UI redesign + in-terminal config wizard.** Originally scoped to the first-run wizard + `/config` + `/login`. Scope expanded through four hotfixes: rebuilt the picker on Ink + React (eliminating raw-stdout redraw bugs), then pulled the full chat REPL onto Ink — closing the split aesthetic where picker chrome was Ink but the everyday turn loop was readline. CLI/server env separation landed in the same cycle: CLI now reads credentials only from `~/.config/brainrouter/config.json`; MCP child stderr is piped so logs don't bleed into the Ink UI. Multi-MCP pool (`McpClientPool`) and identity-based tool prefix (`mcp__brainrouter__<tool>` for servers with `identity: "brainrouter"`) and the multi-profile `/config` MCP manager also shipped here.

**7. CLI Multi-Agent Phase 1 — registry foundations.** Built-in agent definitions (`explorer`, `architect`, `reviewer`, `worker`, `verifier`) moved from hardcoded TypeScript to JSON files under `brainrouter-cli/agents/`. Three-tier loader merge: built-in → user-global (`~/.config/brainrouter/agents/`) → workspace (`.brainrouter/agents/`), workspace wins. `spawn_agent` gains optional `agentId` for registry lookup. Spawn tier hierarchy enforced: `chat` → any; `reasoning` → `worker` only; `worker` → no delegation. Depth capped at 3 (`BRAINROUTER_MAX_SPAWN_DEPTH`). New `/agents defs` lists all definitions with source path.

**8. API key polish.** Three pre-ship bugs fixed: `/config provider <id>` now prompts for the new provider's API key instead of silently reusing the old one; `/login` gained an LLM credential step (skippable) after the MCP transport block; `loadConfig` env fallback expanded to match the saved endpoint against `PROVIDER_CATALOG` and fall back to the provider's own env key before generic vars.

**9. `/model` quick-swap picker.** No-arg `/model` opens an arrow-key picker populated from the active endpoint's `/v1/models` response. Default-cursors on the current model; falls back to the static provider catalog with an "(offline catalog)" badge on timeout. `<name>` arg keeps the direct switch for scripting. Selects persist to `config.llm.model` with no restart.

---

## What was cut and why

Items 1–5 (cron `/schedule`, `/release-notes`, hooks doc, Strict Tool-Call Recovery, per-vendor install snippets) were the original "quick wins" theme for this cycle. All five were deferred when Item 6 (the Ink migration) expanded from S to L and then scope-crept to cover the full chat REPL. Keeping the cycle focused on the Ink migration was the right call — shipping a split aesthetic would have been worse. All five carry into [`0.3.8`](0.3.8.md).
