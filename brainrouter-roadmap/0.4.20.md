# 0.4.20 — Surfaces that tell the truth about their own state

> Status: **ACCEPTED — implementation active since 2026-08-04.** Plan approved in
> [ADR-028](../brainrouter-docs/decisions/ADR-028-surfaces-that-tell-the-truth.md).

ADR-038's Planner/Notes tranche is delivered as the first complete product
slice of this roadmap: shared Dashboard/Desktop presentation, canonical CLI
operations, durable scoped sync, authenticated remote Notes editing, connected
issue projection, real PostgreSQL race/rollback scenarios, and a dedicated
cross-host visual/usability release gate. The remaining rows below still track
ADR-028's broader stack, receipt, execution-engine, source-adapter and retention
programme; accepting ADR-038 does not silently mark those unrelated rows done.

## The question this release answers

> Four surfaces claim states they have not established — a stack that was never created, a steer
> that may never have arrived, a panel showing another session's work, and a planner that would
> present ten sources as one list without saying which are stale. What does it take for each to
> report only what it knows?

## Why now

0.4.19's D13 was written the day GitHub's preview was announced, from a changelog and an overview
page, before the CLI reference existed in our research. The model and the review integration are
sound. The part that touches GitHub is wrong in one specific and serious way: **`stack.addlayer`
opens an ordinary pull request and reports that it created a stack layer.**

## Scope

### ADR-035 completion track

The meeting-durability work shares this release train but remains independently reviewable. Its
status is tracked here so future implementation does not appear in the changelog before it ships.

| Slice | State | Exit condition |
|---|---|---|
| D9 durability chunks separate from transcription units | Implemented | Automated host recovery and corruption-safety coverage is green |
| D9 destructive host acceptance | Pending | Kill/reopen recovery passes on Dashboard and Desktop |
| D10 strict capability/checkpoint and gateway transport foundation | Implemented, dormant | Complete contract is present without changing the active batch path |
| D10 real streaming adapter and both-host activation | Pending | Mid-sentence text, reconnect/replay, and visible fallback pass on both hosts |
| D11 browser server-of-record and speech bitrate | Pending | Refused persistence plus origin erasure recovers the complete remote recording |
| D6 accepted-summary retention and deletion | Pending | Audio survives processing and is deleted only by accepted policy, discard, or expiry |

| ID | Deliverable | Depends on |
|---|---|---|
| S0-1 | Remove `stack.addlayer` — it reports success for work it does not do | — |
| S0-2 | Capability detection: `gh` 2.90+, git 2.20+, `gh-stack` installed; cached per workspace | — |
| S1-1 | Typed outcomes for all ten `gh stack` exit codes | S0-2 |
| S1-2 | Hard stops on rebase-conflict (3), rebase-in-progress (7), recovery-needed (10); feature-disabled (9) hides the actions | S1-1 |
| S2-1 | `gh stack` runner: `init` / `add` / `push` / `submit` / `link` | S1-1 |
| S2-2 | Create path rewritten — a layer is genuinely registered as a stack on GitHub | S2-1 |
| S3-1 | Navigation: `view` / `checkout` / `switch` / `up` / `down` / `top` / `bottom` / `trunk` | S2-1 |
| S3-2 | `sync` / `rebase`, human-initiated only, with a preview of which layers move | S2-1 |
| S4-1 | `stack.merge` — top layer plus everything beneath; destructive; names every PR that will land | S2-1 |
| S4-2 | Long timeout; a 90s+ stack merge is *pending*, never *failed* | S4-1 |
| S4-3 | Detect and report stale layers after a mid-stack merge | S4-1 |
| S5-1 | **Plan-phase → layer authoring**: one executable phase = one layer, created as work happens | S2-2 |
| S5-2 | Depth cap (~5) — past it the agent asks whether the rest belongs in a follow-on stack | S5-1 |
| S5-3 | Never stack on a broken base: a failing layer stops authoring, enforced at create time | S5-1 |
| S5-4 | Fold phases that are not independently reviewable, before the human sees the stack | S5-1 |
| S5-5 | Each layer's PR body states its dependency in prose, not just "layer 3 of 5" | S5-1 |
| S5-6 | Post-hoc auto-propose (fallback for unplanned work), gated + once per change | S5-1, S4-1 |
| S5-7 | `cli.buildLoopEmitPr` emits into the current stack, not a competing branch | S5-1 |
| S5-9 | `stack.sync` / `stack.rebase` on instruction, with a preview of which layers get rewritten | S2-1 |
| S7-1 | Message receipt states: `queued` / `delivered` / `acknowledged` / `dropped` | — |
| S7-2 | Steer ids threaded through `reconcile_steer`, so `acknowledged` is evidence not assumption | S7-1 |
| S7-3 | A dropped steer surfaces loudly with resend; never silently discarded | S7-1 |
| S7-4 | Per-message state in the transcript + a pending-steer count | S7-1 |
| S8-1 | Artifacts panel re-fetches on session change — no close/reopen | — |
| S8-2 | Session filter becomes multi-select, defaulting to the current session | S8-1 |
| S8-3 | With >1 session selected, each artifact shows which session produced it | S8-2 |
| S6-1 | Desktop stack panel (read-only) | S3-1 |
| S6-2 | Mutation controls for confirmed operations | S6-1, S4-1 |
| **E — execution engine (currently inert)** | | |
| E1-1 | `runTurn` dispatches on `cli.executionEngine` — the setting finally does something | — |
| E1-2 | Graph path reaches parity: interrupts, tool authorization, receipts | E1-1 |
| E1-3 | Parity test matrix — same scenarios through both engines, fails on divergence | E1-2 |
| E1-4 | Desktop setting under agent runtime, per workspace, described by what each is good at | E1-1 |
| E1-5 | The running engine is visible in the session, where the work happens | E1-1 |
| **P — planner foundations** | | |
| P0-1 | Hybrid logical clock: stamp, compare, merge; property tests against clock skew | — |
| P0-2 | Planner item schema: mirrored vs owned, tombstones, per-field stamps | — |
| P0-3 | Migration: `planner_items`, `planner_outbox`, `planner_blocks` | P0-2 |
| P1-1 | Local store — immediate reads/writes, network never on the path | P0-2 |
| P1-2 | Outbox: ordered per item, idempotency keys, durable across restart | P1-1 |
| P1-3 | Bounded outbox with age-based shedding + visible refresh-from-server | P1-2 |
| P2-1 | Sync endpoints: push operations, pull since cursor | P0-3 |
| P2-2 | Field-level last-writer-wins on HLC | P0-1, P2-1 |
| P2-3 | Conflicts NOT auto-merged: concurrent text, delete-vs-edit | P2-2 |
| P2-4 | Conflict UI — both versions kept, human picks, nothing lost | P2-3 |
| P3-1 | Source adapter interface + per-source freshness | P0-2 |
| P3-2 | Adapters: Track, GitHub issues, GitHub PRs | P3-1 |
| P3-3 | Adapters: review findings, meeting actions | P3-1 |
| P3-4 | Staleness surfaced per source in the view | P3-1 |
| P4-1 | Today view: aggregated, orderable, schedulable | P1-1, P3-2 |
| P4-2 | Timetable: scheduled and unscheduled blocks | P4-1 |
| P4-3 | Planned vs actual; drift as a ratio, never a scoreboard | P4-2 |
| P4-4 | Carry-over recorded, never scolded | P4-1 |
| P5-1 | Bounded planner context block in the agent briefing, with per-source freshness | P4-1 |
| P5-2 | Agent-callable planner actions via the D6 control layer (read + confirmed mutations) | P5-1 |
| P5-3 | Agent proposes a day plan; every mutation confirmed and marked agent-originated | P5-2 |
| P5-6 | Completed items + planned-vs-actual drift feed the ADR-027 D1 debt ledgers | P4-3 |
| P5-4 | Desktop planner surface | P4-2 |
| P5-5 | Dashboard planner surface (same endpoints) | P2-1, P4-2 |
| P5-5 | Retention + compaction at 90 days | P0-3 |

### ADR-038 delivered slice

| Roadmap area | Delivered evidence |
|---|---|
| P0–P2 | Typed item/block wire contract, scoped stores, composite cursors, durable fingerprinted outboxes, remote-clock absorption, causal text/delete conflicts, tombstones, retry and real two-device PostgreSQL convergence |
| P3 | Production connector-issue projection with explicit org/user scope, actionable HTTPS provenance, freshness, blocked state and estimates; Track, PR, finding and meeting adapters remain broader ADR-028 work |
| P4 | One shared 20-item Today/Calendar surface across Dashboard and Desktop, scheduling, carry-over, planned-versus-actual capture, currentness, responsive density and actionable sync |
| P5 surfaces | Shared Notes editing and Planner rendering in both hosts; canonical CLI `/planner`; browser-safe package boundaries and Cloudflare build proof |
| Release proof | Focused/full suites, real PostgreSQL Planner and Notes scenarios, and a retained-artifact Chromium/native-Electron visual/usability workflow with a human review checkpoint |

## Delivery timetable

Sequential, single-track. The stacked-PR and receipt work (S rows) is a focused repair and lands
first; the planner (P rows) is a new persistence and sync layer.

| Week | Focus | Exit condition |
|---|---|---|
| **1** | S0–S2 — remove the lying action, capability detection, exit codes, create path | A layer is genuinely registered as a stack on GitHub |
| **2** | S3–S4 — navigation, sync/rebase, merge | A stack merges; the confirmation names every PR that lands |
| **2.5** | S5 — plan-phase authoring, depth cap, broken-base guard | The agent emits one layer per plan phase and stops at the cap |
| **3** | S5–S8 — auto-propose, receipts, artifacts panel | No surface claims a state it has not established |
| **4** | E — wire the execution engine, parity matrix, settings surface | Choosing `graph` actually runs the graph, and you can see which is running |
| **5** | P0 — HLC, schema, migration | HLC survives adversarial clock-skew property tests |
| **6** | P1 — local store + outbox | Fully usable with the network off; outbox survives restart |
| **7** | P2-1, P2-2 — sync + LWW merge | Two devices converge on non-conflicting edits |
| **8** | P2-3, P2-4 — conflicts that must not auto-merge | Concurrent text loses nothing; delete-vs-edit asks |
| **9** | P3 — adapters + freshness | Six sources in one view, each showing its own age |
| **10** | P4 — planner and timetable | Today view and timetable usable end to end |
| **11** | P5 — agent context, planner actions, surfaces, retention | The agent answers "what am I on today" from real data and proposes without imposing |
| **12** | Multi-device soak | Offline edits on three devices reconciling without loss |

**Weeks 7–8 carry the real risk.** If merge semantics are wrong there, everything after moves.
Weeks 1–4 are well-understood repairs; weeks 9–11 are adapters and UI over a settled core.

**Week 12 is not padding.** Sync bugs surface under conditions unit tests do not reproduce — two
devices offline at once, an edit during a partial drain, a tombstone racing an edit. A week of
deliberate soak is cheaper than finding the same bugs by losing data.

## Owner decisions recorded

| Question | Answer |
|---|---|
| Agent may run `gh stack sync`? | **Yes, on explicit instruction** — never on its own inference |
| Auto-propose stacks? | **Yes**, gated to `shouldStack:true` and once per change |
| Merge queue | **Wait for GitHub's native queue** — no third-party integration |

## Explicitly out of scope

- **A Trunk-specific merge-queue integration.** Waiting for GitHub's first-party support.
- **Reimplementing cascading rebase.** GitHub maintains it; ours would be a worse copy.
- **Cross-fork stacks.** Not supported by the platform.
- **Auto-merging a stack without confirmation**, however green the checks.
- **A local stack database.** `gh stack` owns local tracking; a second store would drift.
- **A "read" receipt that turns green on context inclusion.** Attention is not instrumentable; a
  receipt we cannot substantiate is worse than none.

## Non-negotiables

- `stack.addlayer` is removed before it is replaced. An action that reports success for work it did
  not do gets no deprecation window.
- History rewriting of branches under review is never agent-initiated without explicit instruction,
  and every run previews the layers it will rewrite.
- Auto-propose fires once per change and never for an indivisible one. A proposal that fires often
  is one that gets dismissed reflexively, and then it is worth nothing when it matters.
- A stack is authored one plan phase at a time as the work happens, not cut out of a finished diff.
  Inferred seams are guesses about intent; recorded ones are not.
- Depth is capped. A fifteen-layer stack is less reviewable than one large pull request, with
  fifteen CI runs attached.
- A layer is never created on top of one that failed its checks.
- A stack merge confirmation names **every** pull request that will land, not only the one asked for.
- Capability is detected, not assumed; where the extension is absent, stack actions are hidden
  rather than offered-and-failing.
- A message never shows "read". It shows `delivered` — what we can prove — and `acknowledged` only
  on evidence the agent consumed it. A dropped steer is always loud.
- The artifacts panel never shows another session's work while claiming to show yours, and a
  multi-session view always attributes each artifact to its source session.
- Planner context reaches the agent as a BOUNDED summary carrying per-source freshness — never the
  whole list, and never without its age.
- The agent never completes a planner item by inference (a merged PR is not a finished todo), and
  never mutates the planner without confirmation.
- A setting that resolves a value nobody reads is a defect, not a stub. `cli.executionEngine` is
  wired, surfaced, and the running engine is visible — or the knob is deleted.
- A module or setting is not done until something calls it. The test proving the caller exists is a
  DIFFERENT test from the one proving the unit works.
