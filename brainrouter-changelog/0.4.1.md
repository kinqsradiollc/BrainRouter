# 0.4.1 — Release candidate (pending test + publish)

The brain-side job queue + agent registry, multi-agent ownership /
budgeting / accounting / supervisor gates / auto-chain, dynamic subagent
overlay + per-child effort, federation work handoff, project scope, and
the `route_agent` retirement. Versions are bumped to 0.4.1; **npm
publish is held** until a full manual test pass.

**Deferred to a 0.4.1 follow-up pass** (each touches a hot or
security-sensitive path and deserves unrushed review):
- **AUG-A2** — apply-time memory dedup (hot capture path + `content_hash`
  column + benchmark).
- **AUG-A3** — modular ranking refactor (the spec assumed a `reranker/`
  dir to split; the scoring logic actually lives across `recall.ts` /
  `neural-spark.ts` / `sqlite.ts`, so a no-behavior-change split needs a
  design pass, not a guess).
- **AUG-A4** — pluggable tracing backend (`cli.tracingBackend`).
- **AUG-A5** — HTTP gateway hardening (scoped tokens, CSRF, CORS, rate
  limits) — security-sensitive.

## Removed

- **`route_agent` (deprecated).** The deprecation window is over —
  `route_agent` is removed in favour of `route_task` (the richer 4-tier
  policy decision shipped in 0.4.0). Role inference (`inferRoleFromTask`)
  is unchanged and still backs `route_task` + batch-spawn routing.

## Fixed

- **`/dm` messages showed a banner but the inbox read back empty.**
  Root cause: the CLI registers + polls its inbox under a per-process
  *federation* key (a UUID, deliberately distinct from the *chat*
  sessionKey so two CLIs in one workspace don't collide), but the LLM
  only sees the chat key — so when an agent called `session_inbox_read`
  it read the wrong inbox and got nothing, even though the banner had
  rendered from the federation-key inbox. Fixed on two fronts:
  - **Agent tool calls now carry the right identity.** At the CLI
    tool-call boundary, `session_inbox_read` / `session_inbox_ack` have
    their `sessionKey` forced to the federation key, and `session_send`
    has its `from` forced — so "check my inbox" reads the inbox the
    message actually landed in, with no key-juggling by the model.
  - **New deterministic `/inbox` command** (counterpart to `/dm` /
    `/broadcast`): reads the current session's inbox with the correct
    key and marks messages delivered. `--peek` inspects without
    consuming; `--all` includes delivered history.

## Added

- **Strict output-contract enforcement (MAS-P3-P3.2).** `wait_agent` /
  `wait_agents --json` now return a parsed `contract` for roles with an
  output contract; `/agent <id>` renders a field-labelled "Contract
  output" block; and `orchestration/synthesis.ts` rolls a finished
  fan-out up by role so the parent composes a clean answer instead of
  pasting raw transcripts.

- **Project (multi-folder) scope (AUG-A1).** A `.brainrouter/project.json`
  marker (`{ "name": "…" }`) names a logical Project spanning several
  workspaces. Records carry an optional `project_tag` (a stable hash of
  the project name via `projectTagFromName`), and `memory_recall` accepts
  `scope: 'project'` + `projectTag` to widen recall from one workspace to
  the whole project — NULL-tolerant like `workspaceTag`, so untagged
  records always surface. `/where` shows the active project. (Mirrors the
  Federation Stage 1 `workspaceTag` contract; capture-time tagging rides
  the same path.)

- **Federation work handoff (FED-S4).** `/handoff <target> [note]` packages
  your current goal + recent transcript into a `goal-handoff` message and
  sends it to another active session; your local goal pauses. The target
  may be a sessionKey, a unique prefix, or `<clientKind>:next-idle` (picks
  the most-idle peer of that kind). The recipient runs `/handoff list` to
  see inbound handoffs and `/handoff accept [fromPrefix]` to adopt one as a
  fresh local goal (the handoff context is attached as a tagged transcript
  entry). *(The interactive "Accept?" auto-prompt and the non-BrainRouter
  `memory_recall` fallback are deferred polish — `/handoff accept` is the
  deterministic, headless-safe path.)*

- **Dynamic subagent overlay + per-child effort (0.4.x-1, 0.4.x-5).**
  `spawn_agent` gains an `overlay` field — a one-off instruction block
  (≤4000 chars) appended to the child's role prompt, the escape hatch
  for a bespoke contractor the five preset roles don't cover; such a
  child is marked `synthetic`. It also gains an `effort` field
  (`low|medium|high`) to override the session `/effort` for that child's
  reasoning.

- **Tool-surface budgeting (MAS-P4-T1).** When an agent's visible MCP
  catalog exceeds `cli.agentMcpToolBudget` (default 40), the tools are
  ranked by relevance to the current task (token overlap with name +
  description) and capped to the budget — keeping prompts lean and tool
  choice sharp. A model call to a trimmed tool returns a structured
  "hidden by budget" hint (not a bare unknown-tool error). Agent
  definitions can also scope their children's MCP surface via
  `toolScope.mcp` + `disallowedTools`.

- **Per-agent accounting (MAS-P4-T3).** Child usage now carries
  `offloadedChars` (output kept out of the parent's context via
  working-memory offload) and `wallClockMs` (spawn→complete). `/tokens`
  renders a per-child "By child" breakdown with those figures,
  `/agents --json` includes full per-child `usage`, and `wait_agents`
  returns a `childTotals` roll-up (tokens, calls, offload savings).

- **Supervisor gates on delegation (MAS-P4-T2).** `/delegation-policy
  [auto | ask-before-spawn | ask-before-write-child | no-children]`
  (persisted per workspace) controls whether — and when — the agent may
  spawn child agents. `no-children` blocks all spawns; `ask-*` policies
  prompt the interactive parent (via the existing yes/no overlay) before
  a top-level spawn (or just write/shell children), and run nested
  spawns inside an approved op without re-asking. Headless `brainrouter
  run` fails an `ask-*` gate closed with a clear "set /delegation-policy
  auto" hint rather than hanging.

- **Auto-chain follow-ups (MAS-P4-T4).** `/auto-chain [review | verify
  | both | off]` — after a `worker` child finishes, automatically chain
  a reviewer (reads the diff) and/or verifier (runs tests/build) on its
  output, closing the "agent shipped, did it actually work?" loop.
  `/auto-review` is now a thin alias (`on` → `review`, `off` → `off`).
  Only workers chain (follow-ups never re-trigger), capped at
  `cli.autoChainMaxFollowups` (default 2). `wait_agents` results and the
  REPL surface a "Follow-up agents: …" line.

- **Multi-agent ownership boundaries (MAS-P3).** When you fan out
  write-capable children with `spawn_agents`, each write/shell child
  must now declare an `ownership` glob (e.g. `src/payments/**`) so two
  parallel children can't clobber the same files — or pass
  `allowOverlap: true` on the entry to opt out. The batch is validated
  up front (no half-spawned fan-out), read-only children are allowed
  with a note, and a child's `write_file` / `edit_file` / `apply_patch`
  are refused outside its glob with a structured error (`apply_patch`
  validates the whole envelope before touching disk, so it's atomic).
  `wait_agents` result objects now include each child's `ownership`.

- **Brain-side job queue + agent registry (BRAIN-P1, partial).** The
  eight pipeline stages that already run inside the memory engine
  (`cognitive_extractor`, `memory_deduper`, `contradiction_checker`,
  `graph_extractor`, `focus_shift_judge`, `focus_distiller`,
  `identity_distiller`, `relevance_judge`) are now formalised as a
  data-driven registry, and every brain-side action can be tracked as a
  durable, retryable row.
  - **`memory_jobs` table** (BRAIN-DESIGN-T2) — `pending → running →
    done|failed|cancelled` lifecycle with priority, attempts, and a
    `runAfter` "do not run before" signal. Cross-process-safe claim
    (`BEGIN IMMEDIATE` under WAL) so federated brain processes can't
    double-pick a job.
  - **Brain agent registry** (BRAIN-DESIGN-T1) —
    `listBrainAgents()` / `findBrainAgentById()` over the eight locked
    agent definitions (id, modelClass, reads/writes, dependsOn,
    idempotencyKey).
  - **Scheduler primitives** — exponential backoff (`2^attempts × 30s
    + jitter`, capped at 5 min) and an idempotency-deduped
    `enqueueAgentJob` that refuses a second job with the same key while
    one is pending/running.
  - **MCP tools** (BRAIN-DESIGN-T3) — `memory_agent_status` (read-only
    per-agent rollup: last status, 24h success rate, pending count),
    `memory_agent_run` (idempotent enqueue), `memory_job_retry`
    (re-arm a failed/cancelled job).

- **Brain jobs accrue from live conversation (BRAIN-P1-T3).** The
  capture pipeline now records every stage run as a durable
  `memory_jobs` row via a synchronous `runAsJob` wrapper — extractor,
  deduper, contradiction checker, graph extractor, focus shift/distill,
  identity distiller. Behaviour is unchanged (same inline scheduling,
  same fire-and-forget error logging); each stage is simply observable
  now. A failed stage records a terminal `failed` row (no background
  runner yet, so no misleading re-arm).

- **`/brain` CLI commands (BRAIN-P1-T5, CLI surface).**
  - `/brain` / `/brain agents` — per-agent table: model class, last job
    status (age-stamped), 24h success rate, pending count.
  - `/brain run <agentId>` — manually enqueue a brain-agent run.
  - `/brain retry <jobId>` — re-arm a failed/cancelled job.

- **Async job runner (BRAIN-P1).** A background loop (started by the
  engine, `unref`'d timer, reentrancy-guarded like the sweepers) drains
  out-of-band jobs enqueued via `memory_agent_run` / `/brain run`:
  sweep stuck locks → claim eligible (cross-process-safe) → execute →
  `done` with output, or re-arm with backoff then terminal `failed`.
  On-demand executors are bound for the per-user synthesis distillers
  (`identity_distiller`, `focus_distiller`) — so `/brain run
  identity_distiller` actually re-distills. Manually running an
  inline-only agent (e.g. `cognitive_extractor`, which runs during
  capture) is cancelled with a clear reason rather than hanging.
  Disable with `BRAINROUTER_JOB_RUNNER=off`.

- **Dashboard "Brain Agents" health card (BRAIN-P1-T5).** A new
  `GET /api/brain/agents` route (+ `GET /api/brain/jobs`) exposes the
  same `BrainAgentStatus[]` the `memory_agent_status` tool returns
  (shared builder), and the dashboard Overview page now renders a Brain
  Agents card — per-agent last status, 24h success rate, and pending
  count, polled every 10s. This completes the BRAIN-P1 surface
  (CLI `/brain` + dashboard).
