// Barrel for the `roles` concern of orchestration: built-in agent roles and the
// typed output contracts each role is asked to produce.
export * from './roles.js';
export * from './outputContracts.js';
