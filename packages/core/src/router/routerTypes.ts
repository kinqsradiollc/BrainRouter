/**
 * Compatibility façade. Canonical provider routing types live under
 * `provider/routing`; keep supported router imports stable during migration.
 */
export * from '../provider/routing/types.js';
