// Goal model concern: the persistent goal/continuation contract — value types,
// budget shape, lifecycle-status enum, length/conflict errors, the storage
// scope resolver, and the shared constants/formatters. Foundational; every
// other goal concern depends on these types.
export * from './goalModel.js';
