// Barrel — MCP progressive-discovery search core (dependency-free catalog
// ranker). Grouped into its own concern folder so the pure keyword ranker stays
// separable from the live client/pool wiring.
export * from './discovery.js';
