/**
 * Public review-domain ports.
 *
 * Implementations live in backend, CLI, Desktop, or test adapters; Core exports
 * only the host-neutral capability contracts.
 */

export * from './assurance.js';
