// Connector retrieval surface: bridge ingested connector documents into the
// memory pipeline and serve slim (metadata-only) document lookups.
export * from './memoryBridge.js';
export * from './slimRetrieval.js';
