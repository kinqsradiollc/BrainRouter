# `/design study` — extract the DNA of a reference

The verb's full protocol lives in [`../study.md`](../study.md); this file exists so every routed verb has one reference under `verbs/`. Load `../study.md` and follow it: source-mode detection (image vs URL), the extraction pass, the diagnosis report, and the three follow-ups (build with the DNA, lock it into `design.md`, or stop).

**Edits the target:** no · **Runs `design_detect` first:** no · **Mode:** not applicable — a diagnosis has no mode.
