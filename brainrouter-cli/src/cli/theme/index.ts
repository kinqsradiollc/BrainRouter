/**
 * Terminal theme concern — semantic color tokens + box-drawing constants
 * shared across every visible CLI surface. See `theme.ts` for the palette
 * definitions and resolution precedence.
 */
export * from './theme.js';
