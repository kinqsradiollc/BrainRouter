/**
 * runtime/federation — the CLI side of the active-session registry plus the
 * compact inbox pane that groups waiting messages by kind.
 */
export * from './federationRegistration.js';
export * from './inboxView.js';
