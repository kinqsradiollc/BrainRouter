// MIGRATION SHIM (ADR-003) — this module moved to @kinqs/brainrouter-core.
// Re-exported here so the CLI's internal importers + tests stay untouched.
export * from '@kinqs/brainrouter-core/dist/config/permissionRules.js';
